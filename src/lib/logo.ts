// Served as a plain static file from /public so it works on any host
// (Lovable's `/__l5e/assets-v1/...` proxy only exists inside Lovable's own
// infrastructure and returns 404 anywhere else, e.g. a self-hosted VPS).
export const logoUrl: string = "/logo-prime.jpeg";
